# MLB HR Model — AI Picks Card
**Slate:** 2026-08-06  
**Published:** 2026-08-06T21:20:43+00:00Z  _(revision — overwrote an earlier card for this slate)_

---

mlbapp@mlb-model:~/mlb-webapp/frontend$ grep -n "bare\|MUST\|FLASH\|HR PICK\|HIT PICK\|COMBO\|BET" /home/mlbapp/mlb-webapp/frontend/src/App.jsx | grep -v "//\|History\|must play\|mustPlays\|flash_\|combo\|bet of\|hr_\|hit_" | head -20
337:    if (inTable && cell0.includes("TOP HR PICKS")) break;
1423:        const bare = clean.trim();
1424:        if (bare.startsWith("🔥") && bare.includes("MUST"))           h1 = [bare, bare];
1425:        else if (bare.startsWith("🔥") && bare.includes("FLASH"))     h1 = [bare, bare];
1426:        else if (bare.startsWith("⚾") && bare.includes("HR PICK"))   h1 = [bare, bare];
1427:        else if (bare.startsWith("🎯") && bare.includes("HIT PICK")) h1 = [bare, bare];
1428:        else if (bare.startsWith("💥") && bare.includes("COMBO"))     h1 = [bare, bare];
1429:        else if (bare.startsWith("💵") && bare.includes("BET"))       h1 = [bare, bare];
1541:    if (text.includes('🟢')||text.includes('MUST')) return {background:'rgba(0,232,122,.12)',color:'var(--green)',border:'1px solid rgba(0,232,122,.3)'};
1589:    const isMust = block.lines.some(l => l.includes('MUST PLAY') || l.includes('AUTO'));
1612:                {isMust&&<span className="badge badge-green" style={{fontSize:9}}>🟢 MUST PLAY</span>}
1680:          {/* MUST PLAYS */}
1695:                    const isName = p.startsWith("**") && (p.includes("MUST") || p.includes("PLAY") || p.includes("Conv") || p.match(/\*\*[A-Z][a-z]/));
1712:          {/* HR PICKS */}
1722:          {/* HIT PICKS */}
1732:          {/* FLASH TABLE */}
1747:                        const isMust = playCell.includes('🟢')||playCell.includes('MUST');
1777:          {/* COMBOS */}
1806:                {sections.bet.split("\n\n").filter(p=>p.trim()&&!p.includes("💵 BET OF THE DAY")).map((para,i)=>{
mlbapp@mlb-model:~/mlb-webapp/frontend$