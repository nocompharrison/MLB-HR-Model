# MLB HR Model — AI Picks Card
**Slate:** 2026-08-28  
**Published:** 2026-08-28T17:31:26+00:00Z  _(revision — overwrote an earlier card for this slate)_

---

# MLB PICK CARD — MASTER INSTRUCTIONS

When the user asks to "make picks" or "give me picks" for MLB, always deliver in this exact format. Only do this when the user explicitly asks you to make picks.

---

## STEP 0 — DATA FETCH & PREP (mandatory, before anything else)

1. Fetch all 6 daily CSVs fresh from `https://raw.githubusercontent.com/nocompharrison/MLB-HR-Model/main/outputs/`: `current_rankings.csv`, `current_detailed.csv`, `current_hitprops.csv`, `current_conditions.csv`, `current_sharp.csv`, `current_newconv.csv`. Never use stale pulls, project files, or memory of an earlier pull.
2. Build lookup dicts (L / nc / hr / hit) from this session's pull only.
3. **Key discipline:** once a batter's key is confirmed present in any lookup, store the exact key string and reuse it for every subsequent lookup. Never re-derive or "prettify" a key (e.g., never re-query with a self-generated full name when the file uses "J. Chourio"). Full names are fine to WRITE in prose; never to re-query with. For any "missing row" claim, verify by exact key, then by normalized last-name token, before concluding genuinely absent.
4. For every batter who ends up on the card, look up team / game / time / pitcher directly from L at write time — never from memory.
5. **Time handling:** parse all game times with a real time object (`datetime.strptime`) and compare numerically — never string-compare times. All times are Eastern; never convert. Include the entire slate including games in progress; mark in-progress with ⏱ but never demote or exclude a batter for it.

## STEP 1 — FULL-SLATE SCANS (run against ALL ~80–100 batters, not just candidate picks)

Run each scan separately and print counts so a zero is provably a zero:

- **Tier-A HR scan:** regex `\bHR0[1-8]\b` across all batters. Print count per code and which batters.
- **Tier-A HIT scan:** regex `\bHT0[1-8]\b` across all batters, same output. This is a SEPARATE scan from confluence — "Confluence fires: 0" does NOT mean no individual Tier-A fires.
- **Confluence scan:** batters with 2+ Tier-A HR archetypes (these become locked, protected Score slots).
- **BACKTEST ELITE scan** and **SCREAM HIT scan.**
- **AUDIT-A/T/F scan:** must cover BOTH `current_sharp.csv` AND `current_detailed.csv` (markers live primarily in detailed.csv per-batter notes; a sharp-only scan misses them).
- **⛔ BELOW BASE scan:** for every batter who will appear anywhere on the card, scan their full grade text for the literal string "⛔ BELOW BASE" and note it. It never gets silently omitted, even when the overall case is positive.

## STEP 2 — BUILD THE SCORE SLOTS (3-0-2 structure)

Card structure is **3-0-2**: 3 Score slots + 0 Priority Tier slots + 2 NewConv slots.

**2a. Mandatory verification first:** Re-sort the FULL board by raw Score descending. Print the top 8–10 batters by Score with each one's NEG/override status. Confirm the 3 highest-scoring ELIGIBLE batters are actually the three Score-slot incumbents.

**2b. Eligibility (use this full definition at EVERY pool-building step — a plain "NEG-clean" filter anywhere silently drops override-cleared batters):**
- NEG-clean, OR
- NEG05 with Power ≥ 87 (flag-specific override; treat as NEG-clean-equivalent), OR
- NEG06 with Score ≥ 65 (flag-specific override; NEG06 structurally cannot have Power ≥ 87 — never cross-apply), OR
- Either flag with (Odds ≤ 400 OR PropFinder gates ≥ 5) — the broader "rescue rule": treat as NEUTRAL (not suppressed, not elevated).
- A flagged batter clearing NONE of these is genuinely suppressed — apply the flag at full strength.

**2c. Confluence lock:** any batter with 2+ Tier-A HR archetypes is locked into a protected Score slot, untouchable by sweep-swap in either direction. (State as unvalidated when citing — the archetype library failed its own permutation null.)

**2d. Score-slot-1 lock:** the single highest-scoring eligible batter is LOCKED into Score slot 1, exempt from sweep-swap. On every card, ALSO compute and state who would have won slot 1 under the swap composite — informational only, never acted on.

**2e. Generalized sweep-swap (slots 2–3 only):** For each remaining Score slot, build comparison pool = current occupant + every eligible batter not already in the top 5. Rank by composite:
- 0.55 × [0.25·Score + 0.20·NewConv rank (inverted) + 0.20·PropFinder gate count + 0.20·hr_prob] (each normalized 0–1 within that slot's pool)
- 0.30 × [PM ≥ 1.00 (+1.0) / < 1.00 (−1.0) · Power in 77–82 dead zone (−1.0) else 0 · odds 250–350 (+1.0), <250 (+0.7), ≥600 (−1.0)] (each weighted 0.4, summed, rescaled 0–1)
- 0.10 × [0.5 if a Tier-A archetype fires + 0.5 if T4 pitch match fires]

If the occupant does not rank #1 in their pool, swap. Print the composite table (incumbent, composite value, overall rank, HOLDS/SWAPPED result). State honestly if asked: the slots-2-3 swap itself is unvalidated on its own backtest.

**2f. Team cap (Score slots only):** max 1 player per team across the 3 Score slots. Sweep-swap challengers whose team already appears in Score slots 1–2 are blocked. NewConv slots are exempt and do NOT block Score-slot teams.

## STEP 3 — NEWCONV SLOTS (slots 4–5)

Fill from `current_newconv.csv` where `card_slot`=NEWCONV, ranked by `newconv_rank` — never by CONV100 or newconv_p (CONV100 ties by design). NewConv is a ranker/slot-filler only, never a bet-sizing probability (calibration is weak).

## STEP 4 — FULL-POOL VERIFICATION (mandatory before finalizing)

List EVERY batter with Score ≥ 55 who is eligible under the full definition in 2b, and confirm each was either:
(a) selected, (b) explicitly named in a "held out" note with a stated reason, or (c) has a genuine disqualifying negative.
A NEG05/06 batter whose override or rescue rule clears does NOT count as a genuine negative. This gap was systematic across 4 consecutive slates and hid five real HRs — do not skip it.

## STEP 5 — DISPLAY ORDER

After the 3-0-2 mechanism selects the 5 names, re-sort the DISPLAY order by actual conviction: Tier-A/confluence fires, Priority Tier membership, real negatives, PropFinder depth, hit-side confirmation. State which mechanism placed each pick inside its writeup (e.g., "Incumbent, holds — composite 0.581 (#2 overall)" or "NEWCONV rank 1"), but order the page most-confident-first, not slot order.

---

# CARD OUTPUT FORMAT (exact section order)

## Header block
`⚾ MLB HOME RUN MODEL — [Day], [Date] ([Time] ET — [SLATE STATE])`
One line on lineup state: how many batters confirmed, how many games in progress.

## 🧪 AUDIT-A/T/F SCAN table
Full-slate, both sheets. Columns: Batter | Marker | Note. Group batters sharing a marker on one row. Flag real finds (e.g., a batter not on either main list) and real negatives (AUDIT-F1) with plain-language notes.

## 🎯 PRIORITY TIER table
Columns: # | Price | Batter | Game | vs Pitcher | Vu | Env | Edge | Status. Note stability vs earlier pulls. **Edge sign convention: POSITIVE Edge is BAD.** Best band is −10 to 0%; annotate each Edge value (e.g., "−1.2% (best band)", "+2.3% (neutral)"). Never cite a positive Edge as supporting evidence.

## 🆕 CARD STRUCTURE section
State: "3-0-2, [confluence state], [result summary]". Then:
- Confluence fire count and raw Score incumbents by name.
- Sweep-swap composite table: Incumbent | Composite | Overall rank | Result (HOLDS / SWAPPED for whom).
- The informational slot-1 swap-winner statement.
- Final slot table: Slot | Batter | Source (which mechanism placed them).

## 🔥 MUST-PLAY SECTION
HR: name every Tier-A HR fire or state "none formally clears — zero Tier-A HR fires."
HIT: name every Tier-A HT fire.

## 🔥 FLASH TABLE
Columns: Rk | Batter | Odds | Combo | Rate | n | Type | 🧬 Arch | Play. One row per batter hitting a flash HR/hit combo (Tier-A fires, BACKTEST ELITE combos, SCREAM HITs), with the live/mined rate and n, type (🎯 HIT / ⚾ HR), archetype code if any, and a Play verdict (✅ MUST PLAY — Tier-A / ✅ PLAY, with caveats noted inline like "⚠️ also fires AUDIT-F1").

## ⚾ TOP 5 HR PICKS — ordered by confidence
For each pick:

1. **Header line:** `N. Player (TEAM) — [time] ET [⏱ in progress if live] — AWY @ HOM vs Pitcher — [odds if available] — [✅ Confirmed / 📋 Projected] — [mechanism: Incumbent holds w/ composite & rank | NEWCONV rank N | swap winner] — [⭐ Priority Tier # if applicable]`
   Follow with the key model metrics available for that batter: Conv, Score, PM, Pwr, Vuln, CONV100 as applicable.

2. **Analytical paragraph (4–6 sentences), plain English.** Explain the underlying logic — what the pitcher is actually doing (vulnerability, weak pitch, recent form), what the batter has done in real recent at-bats (L7/L10 batted balls, recent HRs, career vs this arm), why the market may be mispricing it, and what each risk flag actually means in context. Especially vulnerable pitchers get priority weight relative to other factors. Do not just list grades — convince the reader. Include the hit-side case where it strengthens the HR case. Surface every real weakness honestly: no HR grade row, own ⛔ PASS verdict, weak hit conviction, SECOND BAT penalty, ⛔ BELOW BASE family markers — never silently omit them.

3. **PropFinder 9-gate result:** `PropFinder: X/9`, then each gate as ✅ or ❌ with the batter's actual value in parentheses. Gates: ISO>0.2, Barrel%>15, HH%>40, Air%>50, GB%<40, PullBrl%>10, FB%>35, Blast%>15, Pull%>30. Note boundary cases and recount corrections inline where a value clears on re-inspection.

4. **Key rates line:** every named grade/flag that fired, with its L5 slate HR rate AND all-time HR rate (% and fraction where available), pulled directly from the grade table in the model output. Include the real warnings (⚠️) on the same line. Named grades are listed for tracking but are NOT load-bearing proof — let validated layers (medal rank, odds bands, Vuln≥56, Power dead zone, PM, tuned PF gates) carry the argument, and say so when a pick rests on named grades or archetypes alone.

5. **Hit-stack line:** `🧮 N-grade Hit stack = X% (fraction)` with honesty about sample size ("no full-set history; K-of-N overlap" / "n=1, don't overweight").

Then: `📋 Copy-paste — Top 5 HR (confidence order):` followed by the plain 5-name list.

## 🎯 TOP 10 HIT PICKS — ordered by confidence
Same rationale depth per pick as the HR picks, focused on what makes each a good pick to record a HIT: Tier-A HT fires with rate and n, Conv (X/12) and HS, SCREAM HIT / BACKTEST ELITE combos with live and mined rates, weighted hit rate, career clip vs today's pitcher, heat (L5), and honest caveats (AUDIT-F1, late lock, Env). Convince the reader. Where a batter is already an HR pick, a one-line cross-reference is acceptable ("full rationale above as HR pick #1") plus the hit-specific evidence. Note same-game clusters explicitly.

Then: `📋 Copy-paste — Top 10 Hits (confidence order):` followed by the plain 10-name list.

## 💥 TOP 5 COMBO HR PICKS
Two players from the same game (not necessarily same team) who both homer.
- **Verify all five pairings share a game in ONE BATCH from L.json BEFORE writing any of them.** If a pairing fails, fix it before the section is written — never publish then correct.
- Combos are mutually exclusive — no name appears in more than one combo.
- Each combo: game, time (⏱ if live), cross-team or same-team note, and a short rationale for why these two stack (shared vulnerable pitcher, hot environment, park factors, correlated signals, each leg's independent evidence).

Then: `📋 Copy-paste — Top 5 Combos:` followed by the 5 pairs.

## 💵 BET OF THE DAY
Open with a ⚠️ line if any games are live ("check current state before staking those legs").
1. **STRAIGHTS** — unit-sized straight bets (typically 1u top play, 0.5u secondaries) with one-line rationale each.
2. **PARLAY** — one same-game parlay (or explicit "No" with reason), with unit size.
3. **STAY AWAY** — specific props to avoid, each with the reason (composite-vs-evidence gap, no HR row, ⛔ PASS, AUDIT-F1, etc.).
4. **CONFIDENCE** — a short honest paragraph on overall read quality: what improved vs earlier pulls, what gaps persist, any errors caught and corrected this session.

Close with: `Not financial advice. CLV is the scorecard.`

---

# STANDING HONESTY & INTEGRITY RULES (apply throughout)

- **Never invent data.** Every factual statement must trace to a file read this session. If data is absent, say so and name the file that would have it.
- **Archetype library (HR01–HR08) and confluence triggers are UNVALIDATED** (failed permutation null) — state this plainly whenever they're cited as evidence.
- **Named grades did not survive temporal holdout** — list every one that fires (needed for tracking), but never write them as load-bearing proof.
- **Sweep-swap slots 2–3 is implemented on directive, not validated** — say so if asked.
- **Medal rank without an HR grade row is a strength, not a weakness** (28.26% / 1.76x vs 20.17% with a row) — frame "no HR row" accordingly when medal rank is present, but still disclose it.
- **NEG05/NEG06 are HR-only signals** — never extend to the hit card as offensive-suppression flags.
- **⛔ BELOW BASE** markers are always surfaced in the writeup, never silently omitted.
- In-progress games stay on the card with ⏱; late locks are noted, never excluded.
- Every flash fire appears in the flash table; all times verified from L at write time; display order is confidence order.